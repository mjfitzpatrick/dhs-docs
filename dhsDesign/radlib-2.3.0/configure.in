# Generated by autoscan
# Process this file with autoconf to produce a configure script.

AC_PREREQ(2.5)
AC_INIT([radlib],[2.3.0],[mteel@users.sourceforge.net])
AC_CONFIG_SRCDIR([h/radmsgLog.h])
AM_INIT_AUTOMAKE([radlib],[2.3.0])
AM_CONFIG_HEADER([config.h])

AC_ARG_ENABLE(mysql,
[  --enable-mysql                 include radlib MySQL database support],
[case "${enableval}" in
  yes) mysql=true ;;
  no)  mysql=false ;;
  *) mysql=false ;;
esac],[mysql=false])
AM_CONDITIONAL(MYSQL, test x$mysql = xtrue)

AC_ARG_ENABLE(pgresql,
[  --enable-pgresql               include radlib postgreSQL database support],
[case "${enableval}" in
  yes) pgresql=true ;;
  no)  pgresql=false ;;
  *) pgresql=false ;;
esac],[pgresql=false])
AM_CONDITIONAL(PGRESQL, test x$pgresql = xtrue)


# Checks for programs.
AC_PROG_CC
AC_PROG_INSTALL
AC_PROG_LN_S
AC_PROG_RANLIB

# Checks for libraries.
AC_CHECK_LIB([c], [strncpy])
AC_CHECK_LIB([m], [exp])

# Checks for header files.
AC_HEADER_STDC
AC_HEADER_SYS_WAIT
AC_CHECK_HEADERS([fcntl.h \
                  stdlib.h \
                  string.h \
                  sys/time.h \
                  syslog.h \
                  termios.h \
                  unistd.h])

# Checks for typedefs, structures, and compiler characteristics.
AC_C_CONST
AC_TYPE_PID_T
AC_HEADER_TIME
AC_STRUCT_TM

# Checks for library functions.
AC_FUNC_FORK
AC_FUNC_MALLOC
AC_FUNC_SELECT_ARGTYPES
AC_TYPE_SIGNAL
AC_FUNC_STAT
AC_FUNC_VPRINTF
AC_CHECK_FUNCS([getcwd \
                gethostname \
                gettimeofday \
                memset \
                mkdir \
                mkfifo \
                select \
                strchr \
                strerror \
                strrchr])

AC_CONFIG_FILES([Makefile])
AC_OUTPUT
